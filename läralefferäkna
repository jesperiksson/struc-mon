%%% Generera 200 st intervaller av 4

intervall=zeros(200,4);
for i=1:200
    int=randi([-100 100]);
    intervall(i,1)=randi([-100 100]);
    for n=2:4
        intervall(i,n)= intervall(i,n-1)+int;
    end
end
csvwrite('intervall',intervall)
